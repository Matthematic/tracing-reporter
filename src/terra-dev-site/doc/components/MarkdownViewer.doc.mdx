import MarkdownViewerExample from '../examples/MarkdownViewer?dev-site-example';
import MarkdownPropsTable from '../../../components/MarkdownViewer?dev-site-props-table';
import TracingReport from '!raw-loader!../../../../demos/sort_by_id.md';

# MarkdownViewer

The MarkdownViewer component is a React component that lets you render a markdown tracing report inside of your dev site. It leverages terra-markdown but adds some extra functionality 
such as auto-scrolling and highlighting of selected tables.

# Props
<MarkdownPropsTable />

# Example
<MarkdownViewerExample />
